{{- if not .Values.ca.existingSecret }}
{{- fail "ca.existingSecret is required: the portal will not start without a CA" }}
{{- end }}
{{- if not .Values.openvpn.existingSecret }}
{{- fail "openvpn.existingSecret is required: provide the OpenVPN server keypair" }}
{{- end }}
{{- if not .Values.auth.existingSecret }}
{{- fail "auth.existingSecret is required: it holds the OIDC client secret and the session key" }}
{{- end }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "gruff.fullname" . }}
  labels:
    app: {{ template "gruff.fullname" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ template "gruff.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ template "gruff.fullname" . }}
      annotations:
        checksum/config: {{ .Values.config | sha256sum }}
    spec:
      securityContext:
{{ toYaml .Values.podSecurityContext | indent 8 }}
      containers:
      # Gruff authenticates users itself, so this is the only web container and
      # it is meant to be reachable.
      - name: gruff
        image: {{ .Values.image.gruff }}
        args:
        - --config=/etc/config/gruff.yml
        ports:
        - name: http
          containerPort: {{ .Values.service.gruff.port }}
        env:
        - name: GRUFF_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: {{ .Values.auth.existingSecret }}
              key: {{ .Values.auth.clientSecretKey }}
        volumeMounts:
        - name: config
          mountPath: /etc/config
          readOnly: true
        - name: auth
          mountPath: /etc/gruff/auth
          readOnly: true
        - name: ca
          mountPath: /etc/tls/ca
          readOnly: true
        - name: profiles
          mountPath: /tmp/gruff/profiles
        livenessProbe:
          httpGet: {path: /healthz, port: http}
          initialDelaySeconds: 5
        readinessProbe:
          httpGet: {path: /healthz, port: http}
          initialDelaySeconds: 2
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop: ["ALL"]
        resources:
{{ toYaml .Values.resources.gruff | indent 10 }}

      - name: openvpn
        image: {{ .Values.image.openvpn }}
        env:
        - name: OPENVPN_CONFIG
          value: /etc/config/openvpn.conf
        - name: OPENVPN_DISABLE_FORWARDING
          value: "True"
        ports:
        - name: openvpn
          containerPort: {{ .Values.service.openvpn.internalPort }}
          protocol: UDP
        securityContext:
          # Opening /dev/net/tun and programming routes needs uid 0, so this
          # container opts out of the pod's runAsNonRoot rather than inheriting
          # it - inheriting it and asking for uid 0 is refused outright and the
          # pod never starts.
          runAsNonRoot: false
          runAsUser: 0
          allowPrivilegeEscalation: false
          capabilities:
            # NET_ADMIN alone covers tun setup, routing and iptables.
            drop: ["ALL"]
            add: ["NET_ADMIN"]
        resources:
{{ toYaml .Values.resources.openvpn | indent 10 }}
        volumeMounts:
        - name: config
          mountPath: /etc/config
          readOnly: true
        - name: ca
          mountPath: /etc/tls/ca
          readOnly: true
        - name: openvpn-tls
          mountPath: /etc/tls/openvpn
          readOnly: true
        - name: profiles
          mountPath: /tmp/gruff/profiles
          readOnly: true
      volumes:
      - name: config
        configMap:
          name: {{ template "gruff.fullname" . }}
          defaultMode: 0444
      # 0440, not 0400: a secret volume is owned by root and the group in
      # podSecurityContext.fsGroup, and Gruff does not run as root.
      - name: auth
        secret:
          secretName: {{ .Values.auth.existingSecret }}
          defaultMode: 0440
      - name: ca
        secret:
          secretName: {{ .Values.ca.existingSecret }}
          defaultMode: 0440
      - name: openvpn-tls
        secret:
          secretName: {{ .Values.openvpn.existingSecret }}
          defaultMode: 0440
      - name: profiles
        emptyDir: {}
